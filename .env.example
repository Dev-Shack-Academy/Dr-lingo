
# DR-LINGO ENVIRONMENT CONFIGURATION

#
# Choose the appropriate example file based on your development setup:
#
# 📁 .env.local.example  - For LOCAL development (poetry run, yarn dev)
#                          Copy to: .env AND services/.env
#                          Services: localhost with exposed ports
#
# 📁 .env.docker.example - For DOCKER COMPOSE development (docker-compose up)
#                          Copy to: .env.prod
#                          Services: Docker network with service names
#

# KEY DIFFERENCES

#
# | Setting          | Local (.env)              | Docker (.env.prod)       |
# |------------------|---------------------------|--------------------------|
# | DATABASE_URL     | localhost:5435            | postgres:5432            |
# | REDIS_URL        | localhost:6380            | redis:6379               |
# | RABBITMQ_URL     | localhost:5673            | rabbitmq:5672            |
# | OLLAMA_BASE_URL  | localhost:11434           | ollama:11434             |
# | WHISPER_API_URL  | localhost:9000            | whisper:9000             |
# | VITE_API_URL     | localhost:8000/api        | localhost/api            |
#

# QUICK START

#
# Option 1: Local Development (faster iteration)
#   cp .env.local.example .env
#   cp .env.local.example services/.env
#   cd services && poetry run python manage.py runserver
#   cd client && yarn dev
#
# Option 2: Docker Compose (full stack)
#   cp .env.docker.example .env.prod
#   docker-compose up -d
#   # Access at http://localhost
#
